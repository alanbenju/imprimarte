export type UploadedFile = {
    url: string;
    width: number;
    height: number;
    id: string;
    x: number;
    y: number;
    isDragging: boolean
  }
  
  export type Product = {
    name: string;
    size: string;
    color: string;
    price: string;
  }